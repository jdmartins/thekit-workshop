const fire = require('../firebase');
const auth = fire.auth();

/**
 * Register should
 *
 * getRegister: function
 * Returns page with form for registration
 *
 * createUser: function
 * should add user to the firebase auth
 *
 *
 */
